-- P1: replace with V1__competitor.sql, V2__domain_metrics_snapshot.sql, V3__keyword_gap_row.sql.
-- Flyway requires at least one migration to run cleanly; this is a no-op so the app boots.
SELECT 1;
