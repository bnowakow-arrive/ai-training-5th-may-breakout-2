# Copy to .env and fill in. .env is gitignored.

# SEMRush API key for live competitor data.
# Leave blank during early dev — FakeSemRushClient will be used as a fallback.
SEMRUSH_API_KEY=

# Host port for Postgres. Override (e.g. 5433) if 5432 is already in use locally.
POSTGRES_PORT=5432
